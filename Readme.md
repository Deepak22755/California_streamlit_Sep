pip install numpy ---> any version can come
but if you write pip install numpy==2.4.2 ---> only 2.4.2 version will come
pip install numpy>=2.4.2 ---> greater than 2.4.2 version will come
pip install numpy<=2.4.2 --->  2.4.2 or older version will come

anaconda version                         vs code version
   joblib == '1.5.2'                          joblib - 1.6.0
   numpy == '2.3.5'                            numpy - '2.5.3'
   pandas == '2.3.3'                          pandas - '3.0.6'
   sklearn == '1.7.2'
   streamlit == '1.51.0'
